# PROJECT.md — 項目描述

## 背景

RTX 4060 Laptop 8GB 本機上，要一個「能對話的中文語音助手」。audio.cpp 提供純 C++/ggml 的本地音訊推理引擎（ASR/TTS），但單模型整段式流程延遲高（10s+）。本項目把「識別、思考、合成、播放」四段流水線重構成**可重疊的並行流**，把感知延遲壓到首音 ~3s、句間無縫。

## 目標

- 全本地推理（GPU），不依賴雲端 TTS
- 語音識別快而準（SenseVoice，15x 實時）
- 思考與合成**重疊**（邊想邊播），消除「聽-等-聽-等」的斷感
- 支援聲音克隆（自訂參考音）
- 定版一鍵可用，配置單一檔案

## 架構

```
                 ┌──────────────────────────────────────────┐
   麥克風 ──16k PCM──▶ │ audio.cpp server (8081)                 │
   錄音/靜音VAD        │  /v1/audio/transcriptions/live          │
      ▲               │  model: sense-asr  (0.25GB)             │
      │               └──────────────┬─────────────────────────┘
      │                              轉寫文字
      │                              ▼
      │                    ┌─────────────────────┐
      │                    │ DeepSeek 流式大腦    │
      │                    │ SSE 邊想邊吐         │
      │                    │ 句子切分器(標點/硬切)│
      │                    └───────┬─────────────┘
      │                             逐句文字
      │                              ▼
      │                    ┌─────────────────────┐
      │                    │ 合成線程(預取)        │
      │                    │ qwen3-tts-06b-base    │
      │                    │ (0.6B, 1.9GB, 克隆)   │
      │                    └───────┬─────────────┘
      │                             音頻PCM
      │                              ▼
      └────────── 喇叭 ◀─── 播放線程(單條持續輸出流, 無縫)
```

**線程模型（關鍵）**：
- **大腦線程**：DeepSeek SSE 流式，按句吐字（標點收尾 + 80 字硬切）
- **合成線程**：預取下一句合成（與播放重疊）
- **主線程播放**：一條 OutputStream 持續出聲，句間不關流 → 無縫

「播放時長 ≥ 生成時長」即無斷感：本地暖機後每句合成 1.3~1.9s、音頻 1.8~3.5s。

## 技術選型

| 組件 | 選擇 | 理由 |
|---|---|---|
| 推理引擎 | audio.cpp (release-0.6.1, CUDA) | 純 C++/ggml，免 Python 推理依賴 |
| ASR | **sense-asr**（SenseVoice-Small q8, 0.25GB） | 中文準、15x 實時、輕 |
| 大腦 | **DeepSeek deepseek-chat**（流式 SSE） | 中文好、快、支援 token 流 |
| TTS | **qwen3-tts-06b-base q8**（1.9GB） | 穩定、克隆好、暖機快 |
| 克隆 | qwen3 base `voice_ref` + `reference_text` | WAV 參考音（音檔須與轉錄逐字一致） |
| 播放 | sounddevice OutputStream + queue | 無縫連續播放 |
| 顯存 | sense(0.25) + 0.6B TTS(1.9+工作區) ≈ 2.5GB | 8GB 卡充裕 |

## 性能基準（RTX 4060 Laptop 實測）

| 項目 | 數據 |
|---|---|
| ASR | 11.76s 音頻 / 0.25~1s（15x 實時） |
| 大腦首句 | ~0.9s（流式，第一句標點到達即吐） |
| 本地 TTS 暖機後每句 | 1.3~1.9s（voice prompt 緩存復用） |
| 首音（整輪） | 本地 ~7-8s（含模型冷載）/ 之後無縫 |
| 句間斷感 | 無（播放覆蓋生成） |
| 顯存 | ~2.5GB（閒置 <0.5GB） |

## 版本記錄

| 版本 | 內容 |
|---|---|
| v1.1 (2026-08-21) | 金鑰移入 `.env`、`reference_text` 支援 `@文件`、語音上限 60s、Banner 動態隱藏停用命令 |
| v1.0 (2026-08-21) | 定版：sense-asr + deepseek 流式 + qwen3-tts-06b-base + 逐句無縫 + 硬切 + 本包獨立化 |
| 前身迭代 | 整段式(10s+) → 雲端TTS混搭(5s) → 路B真流式(首音~3s) |

## 已知限制

- 極短句（音頻 <1s）時，生成 1.3s 蓋不住 → 微卡（可做短句合併優化，未做）
- 首句含模型冷載（7~8s）
- 需 DeepSeek API key（放在 `.env`；大腦是雲端 LLM；TTS/ASR 全本地）
